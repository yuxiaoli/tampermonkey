# Environment Information

- **Operating System**: Windows
- **Shell**: PowerShell
- **Path Hint**: Ensure `node` and `npm` are in your PATH. `bun` was not found, so `npm` was used for initialization.
